import React from 'react'
import style from './hero.module.css'

const Hero = () => {
  return (
    <section className={style.hero}>
      <img src='/bm-logo.gif' alt='' />
    </section>
  );
};

export default Hero
